services:
  - type: web
    name: chess-coach-api
    env: python
    buildCommand: bash install_stockfish.sh && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DB_PATH
        value: /opt/render/project/src/chess_coach.db
